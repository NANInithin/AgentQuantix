"""AgentQuantix - a minimal Hugging Face GGUF quantization agent."""

# The single source of truth for the version: pyproject.toml reads it from
# here via setuptools' dynamic metadata, so the package and the MCP server's
# advertised serverInfo can never disagree again.
__version__ = "0.2.0"
